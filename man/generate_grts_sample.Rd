% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{generate_grts_sample}
\alias{generate_grts_sample}
\title{Generate a GRTS (Generalized Random Tessellation Stratified) sample of H3
cell indices covering a polygon.}
\usage{
generate_grts_sample(wkb_bytes, res, containment, n, global_seed)
}
\description{
Generate a GRTS (Generalized Random Tessellation Stratified) sample of H3
cell indices covering a polygon.
}
\section{Arguments}{
\itemize{
\item \code{wkb_bytes}  – Study area geometry encoded as Well-Known Binary.
\item \code{res}        – H3 resolution (0 = coarsest, 15 = finest).
\item \code{containment}– One of \code{"intersect"}, \code{"centroid"}, \code{"boundary"}, \code{"covers"}.
\item \code{n}          – Desired sample size.
\item \code{global_seed}– Reproducibility seed (passed as f64 from R; values above
2^53 lose precision — prefer seeds within that range).
}
}

\section{Returns}{
A \code{data.frame} with three columns, ordered by ascending sort key (GRTS
visiting order):
\itemize{
\item \code{cell}     – H3 cell identifier as a lowercase hexadecimal string.
\item \code{sort_key} – GRTS sort key as a zero-padded 16-character lowercase hex
string. Zero-padding ensures correct lexicographic ordering
on the R side (e.g. for merging tile results).
\item \code{area_m2} – True area of the cell in m². Because GRTS samples cells
with equal inclusion probability (π_i = n / N), area is NOT
part of the sampling mechanism. It enters only at the
estimation stage: to estimate a population total T, scale
each observation y_i by its cell area A_i and divide by π_i:
T_hat = Σ (y_i * A_i) / π_i
For the Hájek mean estimator the π_i terms cancel and the
result is simply an area-weighted average of the y_i values.
}

The returned \code{data.frame} also carries the following attributes which
provide the quantities needed to reconstruct any design-based estimator:
\itemize{
\item \code{n_cells}     – Total number of H3 cells N covering the study area.
Used to compute π_i = n / N.
\item \code{sum_area_m2}– Sum of all cell areas in m² (i.e. the approximate
area of the study area as seen by the H3 grid).
\item \code{n}           – Requested sample size.
\item \code{resolution}  – H3 resolution used.
\item \code{containment} – Containment mode used.
}
}

