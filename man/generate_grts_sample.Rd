% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{generate_grts_sample}
\alias{generate_grts_sample}
\title{Generate a GRTS (Generalized Random Tessellation Stratified) sample of H3
cell indices covering a polygon.}
\usage{
generate_grts_sample(
  wkb_bytes,
  res,
  containment,
  n,
  global_seed,
  area_correction
)
}
\description{
Generate a GRTS (Generalized Random Tessellation Stratified) sample of H3
cell indices covering a polygon.
}
\section{Arguments}{
\itemize{
\item \code{wkb_bytes}      – Study area geometry encoded as Well-Known Binary.
\item \code{res}            – H3 resolution (0 = coarsest, 15 = finest).
\item \code{containment}    – One of \code{"intersect"}, \code{"centroid"}, \code{"boundary"}, \code{"covers"}.
\item \code{n}              – Desired sample size.
\item \code{global_seed}    – Reproducibility seed (passed as f64 from R; values above
2^53 lose precision — prefer seeds within that range).
\item \code{area_correction}– If \code{false} (default), all H3 cells have equal inclusion
probability pi_i = n / N. Cell areas vary by up to ~1.9x
across the icosahedral projection; this is ignored in the
sampling step but can be corrected at the estimation stage
using the returned \code{area_m2} and \code{ip} columns.
If \code{true}, rejection sampling is applied so that each cell
is accepted with probability area_i / max_area, making the
effective inclusion probability proportional to area:
pi_i = n * area_i / sum(area). A pre-pass over the coverage
is required to find the maximum cell area, so this mode
incurs roughly 2x the runtime of the default mode.
}
}

\section{Returns}{
A \code{data.frame} with four columns ordered by ascending sort key (GRTS order):
\itemize{
\item \code{cell}     – H3 cell identifier as a lowercase hexadecimal string.
\item \code{sort_key} – GRTS sort key as a zero-padded 16-character lowercase hex
string. Zero-padding ensures correct lexicographic ordering
on the R side (e.g. for merging tile results).
\item \code{area_m2} – True area of the sampled cell in m².
\item \code{ip}      – Inclusion probability ip_i of the sampled cell:
area_correction = false: ip_i = n / N  (constant)
area_correction = true:  ip_i = n * area_i / sum(area)
}

Design-based estimators using the returned columns:
HT total:   T_hat   = sum(y_i * area_m2_i / ip_i)
Hajek mean: mu_Hajek = sum(y_i * area_m2_i / ip_i) / sum(area_m2_i / ip_i)

The returned \code{data.frame} also carries the following design attributes:
\itemize{
\item \code{n_cells}        – Total H3 cells N covering the study area.
\item \code{sum_area_m2}    – Sum of all cell areas in m².
\item \code{n}              – Requested sample size.
\item \code{resolution}     – H3 resolution used.
\item \code{containment}    – Containment mode used.
\item \code{area_correction}– Whether area-proportional sampling was applied.
}
}

