% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h3_grts.R
\name{h3_grts}
\alias{h3_grts}
\title{Generate a Spatially Balanced GRTS Sample using H3}
\usage{
h3_grts(
  wkb = NULL,
  cells = NULL,
  n,
  resolution = 5,
  containment = c("centroid", "intersect", "boundary", "covers"),
  seed,
  area_correction = FALSE
)
}
\arguments{
\item{wkb}{Raw byte vector representing Well-Known Binary (WKB) geometry in
WGS84 (EPSG:4326).}

\item{cells}{Raw byte vector of pre-computed H3 cell indices.
Use \code{h3_get_coverage()} to generate them.
If provided, \code{wkb}, \code{resolution}, and \code{containment} are ignored.}

\item{n}{Integer. The target number of sample locations to draw.}

\item{resolution}{The H3 resolution to use (0-15).
Ignored if \code{cells} is provided.}

\item{containment}{The containment mode to use. One of "centroid",
"intersect", "boundary", or "covers". Ignored if \code{cells} is provided.}

\item{seed}{Numeric. A random seed passed to the Rust engine to ensure a
reproducible hierarchical shuffle. The seed must be a whole number,
\verb{>= 0}, and \verb{< 2^53}.}

\item{area_correction}{If \code{FALSE} (default), all H3 cells have equal
inclusion probability pi_i = n / N.
Cell areas vary by up to ~1.9x across the icosahedral projection;
this is ignored in the sampling step but can be corrected at the estimation
stage using the returned \code{area_m2} and \code{ip} columns.
If \code{TRUE}, rejection sampling is applied so that each cell is accepted with
probability area_i / max_area, making the effective inclusion probability
proportional to area: pi_i = n * area_i / sum(area).
A pre-pass over the coverage is required to find the maximum cell area, so
this mode incurs roughly 2x the runtime of the default mode.}
}
\value{
A \code{data.frame} with four columns and \code{n} rows, ordered by ascending
sort key (GRTS visiting order):
\itemize{
\item \code{cell}     – H3 cell identifier as a lowercase hexadecimal string.
\item \code{sort_key} – GRTS sort key as a zero-padded 16-character lowercase hex
string. Zero-padding ensures correct lexicographic ordering
(e.g. for merging tile results).
\item \code{area_m2} – True area of the cell in m².
\item \code{ip}      – Inclusion probability pi_i of the sampled cell:
area_correction = FALSE: pi_i = n / N  (constant)
area_correction = TRUE:  pi_i = n * area_i / sum(area)
}

Use \code{h3o::h3_to_points(h3o::h3_from_strings())} to convert the \code{cell} strings
back to \code{sf} geometries.

The returned \code{data.frame} also carries the following attributes which
provide the quantities needed to reconstruct design-based estimators:
\itemize{
\item \code{n_cells}     – Total number of H3 cells N covering the study area.
Used to compute pi_i = n / N.
\item \code{sum_area_m2}– Sum of all cell areas in m² (i.e. the approximate
area of the study area as seen by the H3 grid).
\item \code{n}           – Requested sample size.
\item \code{resolution}  – H3 resolution used.
\item \code{containment} – Containment mode used.
\item \code{area_correction} – Whether area-proportional sampling was applied.
}
}
\description{
Generates a continuous Generalized Random-Tessellation Stratified (GRTS)
sample.
The function can accept either a geometry (\code{wkb}) to tessellate
on the fly, or a pre-computed raw byte vector of H3 cells (\code{cells}).
}
\examples{
\dontrun{
library(sf)
library(h3o)

# Load the built-in North Carolina dataset from the sf package
nc <- st_read(system.file("shape/nc.shp", package = "sf"), quiet = TRUE)

# Use the first county (Ashe) as our study area
study_area <- nc[1, ] |> st_geometry() |> st_as_binary(EWKB = FALSE)

# Draw a sample of 25 points at resolution 7
sample_df <- h3_grts(
  wkb = study_area[[1]],
  n = 25,
  resolution = 7,
  containment = "centroid",
  seed = 123
)

# Convert the H3 indices back into spatial points
sample_df$geom <- h3_to_points(h3_from_strings(sample_df$cell))
sample_df <- st_as_sf(sample_df)

# Visualize the spatially balanced sample
plot(st_geometry(nc[1,]), main = "H3-GRTS Sample (Ashe County)")
plot(st_geometry(sample_df), add = TRUE, pch = 20, col = "red")
}
}
